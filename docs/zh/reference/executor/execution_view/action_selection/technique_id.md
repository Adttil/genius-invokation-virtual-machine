[givm](../../../../reference.md) / [行动选择](../action_selection.md) / **technique_id**

# technique_id

定义于头文件 `<givm/executor.hpp>`

```cpp
attachment_id technique_id() const noexcept;
```

返回当前特技装备的实体 ID。前提为 `has_technique()` 返回 true。
