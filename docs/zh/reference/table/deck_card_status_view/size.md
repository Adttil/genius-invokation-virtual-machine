[givm](../../../reference.md) / [牌桌](../../table.md) / [deck_card_status_view](../deck_card_status_view.md) / **size**

# givm::deck_card_status_view::size

定义于头文件 `<givm/table.hpp>`

```cpp
constexpr size_t size() const;
```

将这个牌库卡牌上的状态作为单实体范围时，取得其中的元素数。

## 返回值

实体有效时返回 1，否则返回 0。
