[givm](../../../reference.md) / [牌桌](../../table.md) / [summon_view](../summon_view.md) / **size**

# givm::summon_view::size

定义于头文件 `<givm/table.hpp>`

```cpp
constexpr size_t size() const;
```

将这个召唤物作为单实体范围时，取得其中的元素数。

## 返回值

实体有效时返回 1，否则返回 0。
