[givm](../../../reference.md) / [牌桌](../../table.md) / [hand_card_status_view](../hand_card_status_view.md) / **size**

# givm::hand_card_status_view::size

定义于头文件 `<givm/table.hpp>`

```cpp
constexpr size_t size() const;
```

将这个手牌上的状态作为单实体范围时，取得其中的元素数。

## 返回值

实体有效时返回 1，否则返回 0。
