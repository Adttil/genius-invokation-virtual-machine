[givm](../../../reference.md) / [牌桌](../../table.md) / [support_view](../support_view.md) / **size**

# givm::support_view::size

定义于头文件 `<givm/table.hpp>`

```cpp
constexpr size_t size() const;
```

将这个支援作为单实体范围时，取得其中的元素数。

## 返回值

实体有效时返回 1，否则返回 0。
