[givm](../../../reference.md) / [牌桌](../../table.md) / [skill_view](../skill_view.md) / **size**

# givm::skill_view::size

定义于头文件 `<givm/table.hpp>`

```cpp
constexpr size_t size() const;
```

将这个技能作为单实体范围时，取得其中的元素数。

## 返回值

实体有效时返回 1，否则返回 0。
