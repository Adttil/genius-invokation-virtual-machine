[返回](../events.md)

# character_defeated

角色已经被写入击倒状态的只读通知。

## 字段

| 字段 | 类型 | 含义 |
| --- | --- | --- |
| `target` | `const character_id` | 已被击倒的角色。 |

## 响应

响应可返回固定入口执行击倒后效果。若被击倒的是出战角色，系统不会因此立即阻塞；真正需要出战角色的后续指令才请求输入。
