[返回](../events.md)

# test_command

用于验证指令执行、event 广播和定义响应分派的测试命令。

## 字段

无。

## 执行

本命令入口不要求额外栈输入。执行期间会临时压入 `test_event` 广播 frame；广播完整结算后弹出该 frame，恢复入口栈形状。

创建 `test_event` 并推进该事件的广播。本命令本身不修改 `table`，响应程序可以通过其领域指令修改对局状态。
